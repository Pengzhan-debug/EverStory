const $ = (selector) => document.querySelector(selector);
const PRESETS = {
  deepseek: { name: "DeepSeek", base_url: "https://api.deepseek.com", model: "deepseek-v4-flash" },
  qwen: { name: "Qwen", base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1", model: "qwen-plus" },
  custom: { name: "Custom API", base_url: "https://example.com/v1", model: "model-id" },
};
const TEXT = {
  "zh-CN": {console:"模型控制台",online:"服务正常",back:"返回游戏",overview:"概览",connections:"API 连接",routing:"智能体路由",diagnostics:"调用检测",security:"凭据安全",securityNote:"密钥仅保存在当前服务进程中，接口不会返回完整密钥。",title:"模型与智能体",subtitle:"管理模型连接、角色分配和运行指标。",mode:"运行模式",calls:"调用次数",tokens:"Token 总量",latency:"平均延迟",last200:"最近 200 次调用",failures:"失败调用",connectionsHelp:"连接可被多个智能体复用，密钥不需要重复填写。",addConnection:"添加连接",routingHelp:"为每个运行角色选择独立或共享的模型连接。",agent:"智能体",group:"分组",state:"状态",assigned:"分配连接",model:"模型",diagnosticsHelp:"显示当前会话最近的模型调用、延迟和 Token 使用。",refresh:"刷新",connection:"连接",result:"结果",noCalls:"暂无调用记录。连接测试或开始游戏后会显示数据。",loaded:"配置已载入。",save:"保存并应用",nameId:"名称 / ID",baseUrl:"接口地址",apiKey:"API 密钥",enterKey:"输入密钥",credentialConfigured:"凭据已配置",noCredential:"未配置凭据",test:"测试",remove:"移除",active:"运行中",planned:"待启用",successful:"次成功",tokensInOut:"输入 / 输出",reviewFailures:"检查最近的失败记录",noFailures:"无失败",failed:"失败",testing:"检测中…",oneConnection:"至少需要保留一个连接。",loadFailed:"无法载入配置",saveFailed:"保存失败",testFailed:"检测失败",investigationTeam:"调查组",gameRuntime:"游戏运行时",liveApi:"在线 API",offlineStub:"离线模拟"},
  en: {console:"Model Console",online:"Service online",back:"Back to game",overview:"Overview",connections:"API connections",routing:"Agent routing",diagnostics:"Diagnostics",security:"Credential security",securityNote:"Keys stay in the current server process and are never returned by the API.",title:"Models and agents",subtitle:"Manage model connections, role routing, and runtime metrics.",mode:"Runtime mode",calls:"API calls",tokens:"Total tokens",latency:"Average latency",last200:"Last 200 calls",failures:"Failed calls",connectionsHelp:"Connections can be shared by agents; credentials are entered once.",addConnection:"Add connection",routingHelp:"Assign an independent or shared model connection to every runtime role.",agent:"Agent",group:"Group",state:"Status",assigned:"Assigned connection",model:"Model",diagnosticsHelp:"Recent model calls, latency, and token usage for this session.",refresh:"Refresh",connection:"Connection",result:"Result",noCalls:"No calls yet. Run a connection test or start the game.",loaded:"Configuration loaded.",save:"Save and apply",nameId:"Name / ID",baseUrl:"Base URL",apiKey:"API key",enterKey:"Enter key",credentialConfigured:"Credential configured",noCredential:"No credential",test:"Test",remove:"Remove",active:"Active",planned:"Planned",successful:"successful",tokensInOut:"in / out",reviewFailures:"Review recent failures",noFailures:"No failures",failed:"Failed",testing:"Testing…",oneConnection:"At least one connection is required.",loadFailed:"Unable to load settings",saveFailed:"Save failed",testFailed:"Test failed",investigationTeam:"Investigation team",gameRuntime:"Game runtime",liveApi:"Live API",offlineStub:"Offline Stub"}
};
let settings = null;
let dirty = false;
function t(key){return(TEXT[$("#language").value]||TEXT["zh-CN"])[key]||key;}
function applyLanguage(){const locale=$("#language").value;document.documentElement.lang=locale;localStorage.setItem("everstory_locale",locale);document.querySelectorAll("[data-i18n]").forEach(el=>{el.textContent=t(el.dataset.i18n);});document.querySelectorAll("[data-i18n-placeholder]").forEach(el=>{el.placeholder=t(el.dataset.i18nPlaceholder);});if(window.opener&&!window.opener.closed)window.opener.EverStoryI18n?.setLocale(locale);}
function message(text,state=""){const el=$("#save-message");el.textContent=text;el.className=state;}
function markDirty(){dirty=true;message($("#language").value==="zh-CN"?"存在未保存的更改。":"Unsaved changes.");}
function esc(value){const el=document.createElement("div");el.textContent=String(value??"");return el.innerHTML;}
function uniqueId(prefix="api"){let n=1,id;do{id=`${prefix}_${n++}`;}while(settings.connections[id]);return id;}
function connectionOptions(selected){return Object.entries(settings.connections).map(([id,c])=>`<option value="${esc(id)}" ${id===selected?"selected":""}>${esc(c.name)} · ${esc(c.model)}</option>`).join("");}
const AGENT_ZH={case_director:"案件主管",field_investigator:"现场调查员",case_analyst:"案件分析师",skeptic:"质疑者",intent_parser:"意图解析器",consistency_judge:"一致性裁判",narrator:"世界叙事者",npc_dialogue:"角色对话"};
function renderConnections(){
  const host=$("#connection-list");host.innerHTML="";
  Object.entries(settings.connections).forEach(([id,c])=>{
    const row=document.createElement("div");row.className="connection-row";row.dataset.connection=id;
    row.innerHTML=`<div class="field"><label data-i18n="nameId">${t("nameId")}</label><input data-field="name" value="${esc(c.name)}"/><div class="test-result">${esc(id)}</div></div><div class="field"><label data-i18n="baseUrl">${t("baseUrl")}</label><input data-field="base_url" value="${esc(c.base_url)}"/></div><div class="field"><label data-i18n="model">${t("model")}</label><input data-field="model" value="${esc(c.model)}"/></div><div class="field"><label data-i18n="apiKey">${t("apiKey")}</label><input class="key-input" data-field="api_key" type="password" ${c.key_configured?`placeholder="${esc(c.masked_key)}"`:`data-i18n-placeholder="enterKey" placeholder="${t("enterKey")}"`}/><div class="test-result ${c.key_configured?"ok":""}" data-i18n="${c.key_configured?"credentialConfigured":"noCredential"}">${c.key_configured?t("credentialConfigured"):t("noCredential")}</div></div><div class="connection-actions"><button class="secondary" data-test type="button" data-i18n="test">${t("test")}</button><button class="secondary" data-remove type="button" data-i18n="remove">${t("remove")}</button></div>`;
    row.querySelectorAll("input").forEach(input=>input.addEventListener("input",markDirty));
    row.querySelector("[data-test]").addEventListener("click",()=>testConnection(id,row));
    row.querySelector("[data-remove]").addEventListener("click",()=>removeConnection(id));
    host.appendChild(row);
  });
}
function renderRoutes(){
  const body=$("#agent-routes");body.innerHTML="";
  settings.agent_catalog.forEach(agent=>{
    const selected=settings.agent_routes[agent.id]||Object.keys(settings.connections)[0];const connection=settings.connections[selected];const row=document.createElement("tr");
    const agentName=$("#language").value==="zh-CN"?(AGENT_ZH[agent.id]||agent.name):agent.name;
    const group=agent.group==="Investigation team"?t("investigationTeam"):agent.group==="Game runtime"?t("gameRuntime"):agent.group;
    row.innerHTML=`<td><span class="agent-name">${esc(agentName)}</span><span class="agent-id">${esc(agent.id)}</span></td><td>${esc(group)}</td><td><span class="badge ${agent.active?"":"pending"}">${agent.active?t("active"):t("planned")}</span></td><td><select data-agent="${esc(agent.id)}">${connectionOptions(selected)}</select></td><td data-agent-model>${esc(connection?.model||"—")}</td>`;
    row.querySelector("select").addEventListener("change",event=>{settings.agent_routes[agent.id]=event.target.value;row.querySelector("[data-agent-model]").textContent=settings.connections[event.target.value]?.model||"—";markDirty();});body.appendChild(row);
  });
}
function renderDiagnostics(){
  const d=settings.diagnostics||{};$("#metric-calls").textContent=(d.calls||0).toLocaleString();$("#metric-success").textContent=`${d.successful_calls||0} ${t("successful")}`;$("#metric-tokens").textContent=(d.total_tokens||0).toLocaleString();$("#metric-token-detail").textContent=$("#language").value==="zh-CN"?`${d.prompt_tokens||0} 输入 / ${d.completion_tokens||0} 输出`:`${d.prompt_tokens||0} in / ${d.completion_tokens||0} out`;$("#metric-latency").textContent=`${d.average_latency_ms||0} ms`;$("#metric-failures").textContent=d.failed_calls||0;$("#metric-health").textContent=d.failed_calls?t("reviewFailures"):t("noFailures");
  const body=$("#recent-calls");body.innerHTML="";(d.recent_calls||[]).forEach(call=>{const row=document.createElement("tr");const agent=$("#language").value==="zh-CN"?(AGENT_ZH[call.agent]||call.agent):call.agent;row.innerHTML=`<td>${esc(agent)}</td><td>${esc(call.connection_id)}</td><td>${esc(call.model)}</td><td>${call.latency_ms} ms</td><td>${call.prompt_tokens+call.completion_tokens}</td><td><span class="badge ${call.ok?"":"bad"}">${call.ok?"OK":t("failed")}</span></td>`;body.appendChild(row);});$("#empty-calls").hidden=Boolean((d.recent_calls||[]).length);
}
function collectPayload(){
  const connections={};document.querySelectorAll(".connection-row").forEach(row=>{const id=row.dataset.connection;connections[id]={name:row.querySelector('[data-field="name"]').value.trim(),base_url:row.querySelector('[data-field="base_url"]').value.trim(),model:row.querySelector('[data-field="model"]').value.trim()};const key=row.querySelector('[data-field="api_key"]').value.trim();if(key)connections[id].api_key=key;});return{mode:$("#mode").value,connections,agent_routes:{...settings.agent_routes}};
}
async function loadSettings(){try{const response=await fetch("/api/llm/settings");if(!response.ok)throw new Error(t("loadFailed"));settings=await response.json();$("#mode").value=settings.mode;renderConnections();renderRoutes();renderDiagnostics();dirty=false;message(t("loaded"),"ok");}catch(error){message(error.message,"bad");$("#service-status").classList.add("bad");}}
async function saveSettings(){const button=$("#save-settings");button.disabled=true;message($("#language").value==="zh-CN"?"正在保存…":"Saving…");try{const response=await fetch("/api/llm/settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(collectPayload())});const data=await response.json();if(!response.ok)throw new Error(data.error||t("saveFailed"));settings=data.settings;$("#mode").value=settings.mode;renderConnections();renderRoutes();renderDiagnostics();dirty=false;message($("#language").value==="zh-CN"?"配置已保存并应用。":"Configuration saved and applied.","ok");return true;}catch(error){message(error.message,"bad");return false;}finally{button.disabled=false;}}
async function testConnection(id,row){if(dirty&&!(await saveSettings()))return;const button=row.querySelector("[data-test]"),result=row.querySelector('.field [data-field="api_key"] + .test-result');button.disabled=true;result.textContent=t("testing");result.className="test-result";try{const response=await fetch("/api/llm/test",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({connection_id:id})});const data=await response.json();if(!response.ok)throw new Error(data.error||t("testFailed"));result.textContent=`OK · ${data.latency_ms} ms`;result.className="test-result ok";await loadSettings();}catch(error){result.textContent=error.message;result.className="test-result bad";}finally{button.disabled=false;}}
function removeConnection(id){if(Object.keys(settings.connections).length===1){message(t("oneConnection"),"bad");return;}delete settings.connections[id];const fallback=Object.keys(settings.connections)[0];Object.keys(settings.agent_routes).forEach(agent=>{if(settings.agent_routes[agent]===id)settings.agent_routes[agent]=fallback;});renderConnections();renderRoutes();markDirty();}
$("#back-to-game").addEventListener("click",event=>{if(window.opener&&!window.opener.closed){event.preventDefault();window.opener.focus();window.close();}});
$("#add-connection").addEventListener("click",()=>{const kind=$("#new-provider").value,preset=PRESETS[kind],id=uniqueId(kind==="custom"?"custom":kind);settings.connections[id]={...preset,key_configured:false,masked_key:""};renderConnections();renderRoutes();markDirty();});
$("#save-settings").addEventListener("click",saveSettings);$("#refresh").addEventListener("click",loadSettings);$("#mode").addEventListener("change",markDirty);const storedLocale=localStorage.getItem("everstory_locale");$("#language").value=storedLocale==="en-US"?"en":(storedLocale||"zh-CN");$("#language").addEventListener("change",()=>{applyLanguage();if(settings){renderRoutes();renderDiagnostics();}});window.addEventListener("storage",event=>{if(event.key==="everstory_locale"){const locale=event.newValue==="en-US"?"en":event.newValue;$("#language").value=locale==="en"?"en":"zh-CN";applyLanguage();if(settings){renderRoutes();renderDiagnostics();}}});document.querySelectorAll(".sidebar a").forEach(link=>link.addEventListener("click",()=>{document.querySelectorAll(".sidebar a").forEach(item=>item.classList.remove("active"));link.classList.add("active");}));applyLanguage();loadSettings();
